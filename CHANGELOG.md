# Changelog

All notable changes to the Cosmos Strategic Capability Platform will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- `projects.delivery_mode` (2026-09-05): a new column (`consultant_guided_async` default / `diy_self_serve` / `live_online`, `CHECK`-constrained, idempotent migration) recording which of three engagement delivery modes a project uses — only `consultant_guided_async` has real behavior behind it; the other two are captured now so project setup doesn't need a second schema/UI change once they're designed. Editable Consultant-only via `PATCH /api/projects/{id}` (`400` on an invalid value; `backend/projects_db.py` gained a `_SELECT_COLUMNS`/`_SELECT_COLUMNS_QUALIFIED` refactor to keep the now-10-column row shape from being repeated across seven call sites). Frontend: a dropdown on the Consultant's project setup page, the two unbuilt options labeled "not yet available." See `documentation/product/roadmap.md`'s "Engagement Delivery Modes" section for the three-way split and a cited architectural precedent (a sibling project, `D:\GitHub\Mental Health App`'s live counselor-AI-co-pilot pattern) for the still-undesigned live mode.

### Documentation
- Engagement Delivery Modes design (2026-09-05): the three-way split above was first written up as a roadmap placeholder — see `documentation/product/roadmap.md`, `documentation/product/stakeholder-clarifications-2026-09.md` §8, and a new Sept 9 discussion item raised as a result — before the minimum placeholder field (above) was actually built the same day.
- Stakeholder clarifications (2026-09-02 through 2026-09-05): cross-checked the Aug 19/24/26 and Sept 2 stakeholder call transcripts against the running code, surfacing several contradictions between the most recent stakeholder direction and what's built — chiefly baseline calibration's position in the flow (built as pre-question; the Sept 2 call discussed moving it post-question, then floated a two-point before/after design instead) and the scope of "remove document upload" (unclear whether it covers all consultant-uploaded artifacts or only the company-background documents feeding calibration). Written up in `documentation/product/stakeholder-clarifications-2026-09.md` (plus a PDF export) as a question list for a 2026-09-09 planning session — nothing in the running code was changed as a result; this is tracked, not yet resolved.
- Full documentation audit and cleanup (2026-09-05): `documentation/README.md`, `documentation/architecture/overview.md`, `documentation/guides/quick-start.md`, `documentation/testing/test-strategy.md`, `documentation/reference/source-materials.md`, `documentation/product/functional-spec.md`, `documentation/product/brd.md`, `README.md`, and this changelog's own `Planned` section were carrying stale status claims left over from as early as 2026-08-24 (a vanilla-JS frontend, no auth, no `responses` table, a 316-test suite, "test bed not yet built," baseline calibration/adaptive difficulty still listed as undone) — all reconciled against `CLAUDE.md` and the live code. `documentation/development/technical-spec.md`'s existing single-section staleness banner was widened to cover the whole document, since its schema/API sections were as out of date as the endpoint list it already flagged.

### Added
- Async artifact ingestion pipeline (2026-09-02) — **built in code, not deployed** (no live GCP deployment exists for anything in this repo). `POST /api/projects/{id}/artifacts` is now dual-mode on `GCS_ARTIFACTS_BUCKET`: **set** → stream the bytes to `gs://cosmos-artifacts-{env}/raw/{project_id}/{artifact_id}/{filename}` (`backend/gcs_artifact_storage.py`), mark the row `Queued`, return immediately, and let an Eventarc object-finalized trigger invoke `backend/processor_main.py` (a second deployable FastAPI service, Cloud Run `cosmos-artifact-processor`) to run the same `ingest_artifact` core, move the object to `processed/`/`failed/`, and record the final status + `gcs_object_path`; **unset** (local dev, CI, the pytest suite) → ingestion runs inline exactly as before with nothing persisted to GCS. `DELETE /api/projects/{id}/artifacts/{artifact_id}` now cascades to the GCS object first (a `NotFound` counts as success). Schema: new `Queued` status value, new nullable `gcs_object_path` column, and `md`/`xlsx` added to `source_format`, all via an idempotent migration in `database.init_db`. New file types: `.md` (reuses the plain-text extractor) and `.xlsx` (`openpyxl`, batched-row chunking that never splits a row across chunks). Frontend: the project setup page polls artifact status and renders the `Queued` state. Infrastructure: `infra/terraform/artifact-pipeline/` — the repo's **first Terraform module** (artifacts bucket, Eventarc trigger, private Cloud Run service, `cosmos-backend-sa`/`cosmos-processor-sa` and their prefix-conditioned IAM). See `docs/superpowers/specs/2026-09-02-artifact-ingestion-pipeline-design.md` and `docs/superpowers/plans/2026-09-02-artifact-ingestion-pipeline.md`.
- Baseline Calibration (Guided Learning Flow, part a): `calibration_concepts` (process-scoped, Consultant-authored via `/api/projects/{id}/framework/calibration`) and `calibration_responses` (project-scoped, COALESCE-upserted) tables; `backend/calibration_db.py`; a `calibration_awaiting_answer` phase prefixed onto `chat_engine.py`'s existing state machine — informational only, never blocks progress to the first question, skipped entirely for projects with no concepts configured or the legacy `case_id` chat mode. Frontend: a "Baseline Calibration" panel on the project setup page (mirrors the Framework editor) and two new chat message types (`calibration_prompt`, `calibration_feedback`). See `docs/superpowers/specs/2026-09-01-baseline-calibration-design.md` and `docs/superpowers/plans/2026-09-01-baseline-calibration.md`.
- Admin UI (2026-08-31): `/admin` page with a Users tab (list/create/toggle-admin/toggle-active/reset-password) and a Projects tab (list all/edit/activate-deactivate/delete/manage members), backed by new admin endpoints — `GET/POST /api/admin/users`, `PATCH /api/admin/users/{id}`, `POST /api/admin/users/{id}/reset-password`, `GET/PATCH/DELETE /api/admin/projects[/{id}]`, `PATCH /api/admin/projects/{id}/status` — all `require_admin`-gated. Also added project membership management: `POST/GET/PATCH/DELETE /api/projects/{id}/members[/{user_id}]` (`require_admin_or_consultant`), closing the "no way to add a ClientUser" gap tracked since Phase B. See `docs/superpowers/specs/2026-08-31-admin-ui-design.md` and `docs/superpowers/plans/2026-08-31-admin-ui.md`.
- Frontend GUI Overhaul (2026-08-31): `frontend-react/` fully wired to the real backend — login/register pages, project dashboards from `GET /api/projects`, a New Project form, a project setup page (industry context, activate, artifacts, members, Framework editor), a client hub, and the chat interview page rendering project-scoped Level 1/2/3 JSON benchmarks as a split-screen comparison with source references, a self-evaluation dropdown, and a Download Brief action. The legacy case-study catalog (`CASES_DATA`, `GET /api/cases`, `GET /api/case/{case_id}`, `POST /api/evaluate`) and the frontend's `mockProjectState.ts` were **retired**, not left in place — see Removed below.
- Framework Authoring Mode: each project now owns its own framework. The seeded "Aditya Birla Brand Compass V2" process is flagged as a read-only `is_template`; `POST /api/projects` clones it into a fresh process (named `"{project.name} Framework"`) and points the project at the clone, and a one-time idempotent migration in `database.init_db` re-points existing projects the same way. New `questions.sequence_order` column (migration + backfill) powers move-up/down. New `backend/framework_db.py` (template lookup, clone, stage/question CRUD + guidance upsert + reordering) and seven Consultant-only endpoints under `/api/projects/{id}/framework` (get, add/patch/delete stage, add/patch/delete question). Evaluation/chat/brief are untouched — they already read `project.process_id`. Frontend: a "Framework" editor on the consultant's project setup page, and the New Project form no longer hardcodes a process id. See `docs/superpowers/specs/2026-09-01-framework-authoring-mode-design.md` and `docs/superpowers/plans/2026-09-01-framework-authoring-mode.md`.
- Backend API Integration: DB-backed `GET /api/process/{process_id}` (stages/questions/guidance nested, `backend/process_db.py`); `RagEngine.search_merged` (merged Framework + Engagement KB retrieval, source-tagged, excluding `case_study_resolution`-purpose chunks) and `RagEngine.generate_comparative_benchmarks` (Level 1/2/3 benchmark prompting, distinct from the existing single-rating prompt); `backend/responses_db.py` (first code to read/write the Phase-B `responses` table, upsertable via `COALESCE` so a separate self-evaluation save never wipes a previously-saved answer); `backend/brief.py` (markdown brief compiler); and three new endpoints — `POST /api/projects/{id}/evaluate`, `POST /api/projects/{id}/responses`, `GET /api/projects/{id}/brief`. Built as **entirely new, additive endpoints** — `POST /api/evaluate`, its response shape, and `CASES_DATA`/`GET /api/cases`/`GET /api/case/{case_id}` are untouched; the actual cutover is Frontend GUI Overhaul's job. See `docs/superpowers/plans/2026-08-28-backend-api-integration.md`.
- Phase C (Engagement Knowledge Base): `project_artifacts` and `project_kb_chunks` (pgvector, HNSW-indexed) tables, `backend/project_artifacts_db.py`, `backend/project_knowledge_base.py` (PDF/DOCX/PPTX/TXT extraction, chunking, AWS Transcribe audio transcription with graceful degradation to a `'Transcript Needed'` status when AWS isn't configured, embedding, and the synchronous `ingest_artifact` orchestrator), and three endpoints on `/api/projects/{id}/artifacts[/{artifact_id}]` — `POST` (upload, Consultant-only, capped at 50MB), `GET` (list, any project member), `DELETE` (Consultant-only, cascades to chunks). Original uploaded file bytes are never persisted — only the extracted/transcribed text survives, embedded into `project_kb_chunks`. At the time this phase landed, retrieval blending and a frontend panel didn't exist yet; both now do (see Backend API Integration and Frontend GUI Overhaul below). See `docs/superpowers/plans/2026-08-28-phase-c-engagement-kb.md`.
- Phase B (Projects): `projects` and `project_members` tables, `backend/projects_db.py`, `require_admin`/`require_project_role`/`require_active_project` FastAPI dependencies (`backend/auth.py`), and five endpoints — `POST /api/projects` (SystemAdmin-only), `GET /api/projects` (list), `GET /api/projects/{id}` (detail), `PATCH /api/projects/{id}`, `POST /api/projects/{id}/activate` (Draft→Active). Also adds the `responses` table as DDL only (project_id-based, replacing the dropped legacy shape) — nothing reads/writes it yet. `CASES_DATA`/`GET /api/cases`/`GET /api/case/{case_id}` are deliberately left in place (the current frontend still depends on them); `POST /api/projects/{id}/members` was not built (only the initial Consultant, assigned atomically at creation, exists so far). None of the new dependencies are wired into any pre-existing endpoint yet. See `docs/superpowers/plans/2026-08-28-phase-b-projects.md`.
- Phase A (Users & Auth): a real `users` table, `POST /api/auth/register`, `POST /api/auth/login` (JWT via `python-jose`, `HS256`, 24h expiry), `GET /api/auth/me`, and a `get_current_user` FastAPI dependency (`backend/auth.py`, `backend/users_db.py`). Password hashing via `passlib[bcrypt]`. See `docs/superpowers/plans/2026-08-28-phase-a-users-auth.md`.
- Pluggable LLM provider abstraction (`backend/llm_providers/`) supporting Anthropic, OpenAI, and Gemini (via Vertex AI), switchable at runtime via `platform_settings` and a stopgap admin-token-gated `/api/admin/settings` endpoint.
- Chat-style interview backend: `chat_sessions`/`chat_messages` schema, a phase state machine (`backend/chat_engine.py`), and three new API endpoints (`POST /api/chat/sessions`, `POST /api/chat/sessions/{id}/messages`, `GET /api/chat/sessions/{id}`) — additive, `/api/evaluate` unchanged.
- Next.js (React, TypeScript) frontend (`frontend-react/`), replacing the old vanilla HTML/CSS/JS frontend — covers all three roles (SystemAdmin, Consultant, ClientUser) and is backed by the chat-style interview API. Projects/Auth (Phase A/B) remain mock/`localStorage`-backed pending those phases landing. See `docs/superpowers/plans/2026-08-26-react-frontend-migration.md`.

### Changed
- Audio transcription (`backend/project_knowledge_base.py::transcribe_audio`) migrated from AWS Transcribe/`boto3` to Google Speech-to-Text (`google-cloud-speech`, `google-cloud-storage`), reconciling a gap where Phase C (below) had added a hard AWS dependency three days after the 2026-08-25 GCP platform decision. `GCS_TRANSCRIBE_BUCKET` replaces `AWS_TRANSCRIBE_S3_BUCKET`; supported formats are mp3/wav/flac/ogg (m4a falls back to `'Transcript Needed'`, same as an unconfigured bucket). The codebase now has no remaining AWS dependency.
- `GET /api/status` now reports `active_llm_provider` instead of AWS Bedrock connection status.
- Database platform migration (Phase 0): `backend/database.py` and `backend/rag_engine.py` moved off SQLite + a flat-file vector JSON onto a single Neon Postgres database with the `pgvector` extension — covers the `processes`/`stages`/`questions`/`guidance` tables and the Framework Knowledge Base (`framework_kb_chunks`). The legacy SQLite `responses` table was dropped, not migrated (superseded by Phase B's `project_id`-based redesign — see `documentation/product/roadmap.md`). Design spec: `docs/superpowers/specs/2026-08-24-neon-postgres-pgvector-design.md`.
- `LLMProvider.complete()` now accepts a multi-turn message history instead of a single user prompt (all three adapters updated).

### Fixed
- Seven cross-task integration bugs in the async ingestion pipeline, found by a final whole-branch review that no single task's review could see (2026-09-02). **Eventarc's GCS-direct triggers support only `bucket`/`type` matching criteria — there is no object-name-prefix filter, which the spec had assumed** — so the processor's own writes to `processed/`/`failed/` re-trigger it; `handle_gcs_event` now returns `200 skipped` for any unparseable/non-`raw/` payload instead of a 500 that Eventarc would retry for days. The processor also accepts `Uploaded` as well as `Queued` (the upload endpoint flips to `Queued` only *after* the GCS write returns, so an event delivered in that window would otherwise strand the artifact); wraps ingest→move→update in `try/except`, moving the object to `failed/` and marking the row `Failed` rather than leaving it stuck at `Processing` forever on a 500-retry loop; handles `ingest_artifact` returning `None` (artifact deleted mid-processing) instead of raising `TypeError`; and loads a bare `SentenceTransformer` rather than a full `RagEngine`, whose `__init__` also runs `load_or_build_index()` and could have a processor cold start write into the *shared* Framework Knowledge Base. `DELETE /api/projects/{id}/artifacts/{artifact_id}` treats a GCS `NotFound` as success so an already-gone object can't make a row permanently undeletable. Terraform: the processor Cloud Run service got `2Gi`/`2` CPU and a `900s` timeout (the 512 MiB default OOMs on the embedding model; the default request timeout cut off audio transcription's ~5-minute polling ceiling) plus the missing `GCS_TRANSCRIBE_BUCKET` env var and an `objectAdmin` binding on that bucket (without which every audio artifact silently degrades to `Transcript Needed`), and `cosmos-backend-sa`'s `raw/` binding was widened from create-only `objectCreator` to `objectAdmin` so the delete endpoint can actually remove a stuck artifact's object.
- A ClientUser landed on the Consultant/Admin project setup UI instead of the client chat flow: `GET /api/projects` and `GET /api/projects/{id}` now include the caller's own membership `role` (missing from both responses since Phase B), and the frontend uses it to route/filter correctly — login redirects to `/client` instead of `/` when the user has no Consultant work, the `/` project list only shows projects where the caller is Consultant, `/client` only shows ones where they're a ClientUser, and the admin project-setup page redirects a ClientUser away if they land on it directly.

### Removed
- AWS Bedrock (`boto3`) dependency from `backend/rag_engine.py`.
- The old vanilla HTML/CSS/JS frontend (`frontend/index.html`, `app.js`, `style.css`) and its static-file mount in `backend/main.py`, superseded by `frontend-react/`.
- **The legacy case-study catalog** (2026-08-31): `CASES_DATA`, `GET /api/cases`, `GET /api/case/{case_id}`, `POST /api/evaluate`, and the frontend's `mockProjectState.ts` — retired once the real `/api/projects/*` backend was fully wired into `frontend-react/` (see Frontend GUI Overhaul above). `chat_sessions` gained a nullable `project_id` column so a chat interview session is always project-scoped now.

### Planned
- Guided Learning Flow, remaining items (baseline concept calibration and adaptive question difficulty are **done** — see Added above): keyword-agnostic answer mapping, a two-case-study resolution flow with a hidden reveal, user-driven self-evaluation with a corpus-relative depth signal, and a module-end Start/Stop/Continue reflection — from an Aug 24 stakeholder review meeting. A 2026-09-02 follow-up call surfaced open questions that may reshape this list before further work starts (calibration's position in the flow, the scope of consultant-uploaded artifacts, product-vs-consulting-aid targeting) — see `documentation/product/stakeholder-clarifications-2026-09.md`, pending a 2026-09-09 planning session. See also `documentation/product/functional-spec.md`.
- Production deployment: a GCP Cloud Run deployment was specced 2026-08-25, but only its CI test workflow (`.github/workflows/ci.yml`) has actually been built. The Dockerfile, `/healthz` route, deploy workflow, GCP project bootstrap, Secret Manager/Workload Identity Federation, and Cloud Monitoring alerts are still just plan steps. The async ingestion pipeline's Terraform module (see Added above) is written and validated but has never been applied to a real GCP project. See `documentation/product/roadmap.md`.

## [0.1.0] - 2026-08-24

### Added
- `CLAUDE.md` — single consolidated project reference (overview, architecture, status, dev workflow, documentation map).
- `documentation/` knowledge base: `product/` (BRD, functional spec, roadmap, concept synthesis), `architecture/overview.md`, `development/technical-spec.md`, `testing/test-strategy.md`, `guides/quick-start.md`, `reference/source-materials.md`, and an index `README.md`.
- This changelog.

### Changed
- `README.md` trimmed to a short public-facing intro pointing to `CLAUDE.md` and `documentation/`.

### Removed
- `plan/` folder — its contents were migrated into `documentation/` (see above); history preserved via git.

---

*Commits before this changelog existed (`954a3f4`, `98548d7`, `661893f`) are not itemized retroactively — see `git log` for that history.*
